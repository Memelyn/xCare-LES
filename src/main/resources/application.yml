spring:
  application:
    name: xCare
  datasource:
    driver-class-name: con.mysql.cj.jdbc.driver
    url: jdbc:mysql://localhost:3306/xCare?useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
