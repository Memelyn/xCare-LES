package com.xCare.repository;
import com.xCare.model.ItemMedico;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ItemMedicoRepository extends JpaRepository<ItemMedico, Long>{
}
